# 🧪 Payment Mock API

Мини-сервис для проверки навыков тестирования REST API (интернет-эквайринг).

## 🚀 Функционал
Сервис реализует упрощённый платёжный процесс:
- `POST /payments/init` — инициализация платежа
- `POST /payments/confirm` — подтверждение платежа
- `POST /payments/refund` — возврат платежа
- `GET /payments/{id}` — получение информации о платеже

⚠️ В коде специально заложены 6 “багов” (расхождения с ТЗ), которые кандидат должен найти.

## 🧰 Технологии
- Java 11
- Spring Boot 2.7
- Lombok
- OpenAPI (Swagger)
- Без базы данных (используется `ConcurrentHashMap`)

## ▶️ Запуск локально
```bash
mvn spring-boot:run
